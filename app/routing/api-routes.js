var app = express();


// get route with url /api/friends this is to display JSON of all possible friends.
// post routes /api/friends handles incoming survey results. this route will also be used to handle the compatability logic.
app.get('/api/friends', function(req, res){
    res
});